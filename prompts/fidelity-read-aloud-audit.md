# Fidelity and read-aloud audit

Compare `{spoken_draft_path}` with `{corrected_path}` section by section and produce `{script_path}`.
This is a text-based speakability audit, not a claim that anyone performed or timed a real reading.

Restore any lost event, example, joke, digression, purposeful repetition, qualification, or approved
correction. Remove invented transitions, explanations, and documentary framing. Fix remaining
phrasing that a native speaker would stumble over or would not naturally say: long noun-heavy
clauses, unclear referents, repeated empty constructions, awkward breaths, formal passives, and
translated word order. Preserve the spoken rewrite where it is faithful and natural.

Ensure every approved correction reads as though it had always been part of the narration. Remove
meta-corrective phrasing, comparisons with Barbero's original assertion, and fact-checking asides.
Do not say that Barbero is wrong or report his old figure before giving the corrected one. Preserve
uncertainty only when the approved proposal substantively requires it, phrased naturally rather
than as an editorial intervention. Retain the `[N-...]` marker for silent traceability.

Audit every non-quotation paragraph for consistent historical present. Preserve names, dates,
numbers, modality, exact eligible quotation wording, coverage comments, and Q/C/N marker sets.
Records marked `not-applicable` or `unavailable` retain Barbero's contextual English rather than
ledger wording. Keep annotations for manual skipping and add no pronunciation cues.

Run editorial validation. Edit only `{script_path}` and do not create a commit.
